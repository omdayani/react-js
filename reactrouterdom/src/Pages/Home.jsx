import React from "react";

const Home = () => {
  return (
    <>
      <section className="flex items-center justify-center h-screen w-full flex-col gap-y-2">
        <h1 className="text-9xl font-bold">Homepage</h1>
        {/* <h1>Steps:</h1>
        <p>1. install react router dom</p>
        <p>2. main.jsx == warp app with BrowserRouter</p>
        <p>3. App.jsx == warp all Route with Routes</p>
        <p>4. App.jsx == Create a Route For All Pages</p>
        <p>5. convert All a == Link</p>
        <p>6. convert all herf == to</p> */}
      </section>
    </>
  );
};

export default Home;
